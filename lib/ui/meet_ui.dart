import 'package:flutter/material.dart';
    
class MeetUi extends StatefulWidget {
  const MeetUi({Key? key}) : super(key: key);

  @override
  _MeetUiState createState() => _MeetUiState();
}

class _MeetUiState extends State<MeetUi> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}