import 'package:flutter/material.dart';
    
class PersonalStudyUi extends StatefulWidget {
  const PersonalStudyUi({Key? key}) : super(key: key);

  @override
  _PersonalStudyUiState createState() => _PersonalStudyUiState();
}

class _PersonalStudyUiState extends State<PersonalStudyUi> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}