import 'package:flutter/material.dart';
    
class BibleUi extends StatefulWidget {
  const BibleUi({Key? key}) : super(key: key);

  @override
  _BibleUiState createState() => _BibleUiState();
}

class _BibleUiState extends State<BibleUi> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}